import { createIcon } from "../create-icon"

export const {{ properCase name }}Icon = createIcon({
  title: "{{properCase name}}Icon",
  viewBox: "0 0 16 16",
  path: (
    /*
     * You should remove this comment.
     *
     * 1. Copy all paths from svg icon into here, you may need to use `<></>` to wrap multi path
     * 2. Replace `viewBox` props with svg icon's `viewBox`
     * 3. Rename `kebabCase` to `camelCase`. e.g. stroke-width -> strokeWidth
     * 4. Check this icon in storybook
     *
     * Done!
     */
  ),
})

{{properCase name}}Icon.displayName = "{{properCase name}}Icon"
